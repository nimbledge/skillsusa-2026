# Weather App

A simple weather app that fetches the location of a zip code from zippopotamus. It then fetches the weather at that location from api.weather.gov. It displays that weather on a simple webapp using the Simple.css framework.
